spring:
  #  application:
  #    name: ms-ilvc-eureka-user-hystrix # 指定应用名称
  #  profiles:
  #    active: prod
  cloud:
    config:
      name: ms-ilvc-eureka-order
      profile: prod
      uri: http://localhost:8889/
      fail-fast: true
      enabled: true